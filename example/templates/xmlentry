<entry>
  <title>{{.Title}}</title>
  <link href="{{(siteConfig).URL}}{{.SitePath}}" />
  <updated>{{.Date}}</updated>
  <id>{{(siteConfig).URL}}{{.SitePath}}</id>
  <author><name>{{(siteConfig).Author}}</name></author>
  <content type="html"><![CDATA[
{{recurse .Content .}}
  ]]></content>
</entry>

